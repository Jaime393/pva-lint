# pva-lint V202 — ρ(x)>0

Linter PVA — Score 0-1 — Transición de fase >0.999

Verifica:
- DOI verificable via doi.org/api/handles
- Datos públicos con DOI/URL
- Código en repo abierto
- Dockerfile reproducible
- make reproduce
- Figuras con código fuente

```bash
pip install requests click
python cli.py --json
python cli.py --path ./paper
```

Parte de PVA ecosistema — 5 capas — validación como transición de fase.
Bloque 1 de 5 — MVP — ρ(x)>0 — El suelo es.
